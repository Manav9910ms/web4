import{NextRequest,NextResponse}from"next/server";import{requireAdmin}from"@/lib/auth";import{adminDb}from"@/lib/firebase-admin";const s=(v:any)=>v?.toDate?v.toDate().toISOString():v;
export async function GET(r:NextRequest){try{await requireAdmin(r);const snap=await adminDb().collection("orders").limit(500).get();return NextResponse.json(snap.docs.map(d=>{const x=d.data();return{id:d.id,...x,createdAt:s(x.createdAt),updatedAt:s(x.updatedAt)}}).sort((a,b)=>String(b.createdAt??"").localeCompare(String(a.createdAt??""))))}catch(e){const m=e instanceof Error?e.message:"Request failed";return NextResponse.json({error:m},{status:m==="UNAUTHENTICATED"?401:m==="FORBIDDEN"?403:500})}}