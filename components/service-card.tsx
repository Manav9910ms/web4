import Link from"next/link";import{ArrowUpRight,Camera,Copy,FileText,FileUser,Monitor,Palette,Printer,ScanLine}from"lucide-react";import type{Service}from"@/types";
const icons={FileText,FileUser,Printer,Palette,ScanLine,Copy,Camera,Monitor};
export function ServiceCard({service}:{service:Service}){const Icon=icons[service.icon as keyof typeof icons]??FileText;return <article className="service-card"><div className="service-icon"><Icon size={22}/></div><div className="service-card-body"><div className="service-title-row"><h3>{service.name}</h3>{service.popular&&<span className="pill">Popular</span>}</div><p>{service.description}</p><div className="service-foot"><strong>₹{service.price}</strong><span>{service.unit}</span><Link href={`/book?service=${service.id}`} className="icon-link"><ArrowUpRight size={18}/></Link></div></div></article>}